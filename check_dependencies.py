try:
  import streamlit
  print("streamlit is already installed.")
except ImportError:
  print("streamlit is not installed.")
 

try:
  import langchain
  print("langchain is already installed.")
except ImportError:
  print("langchain is not installed.")
 

try:
  import langchain_ollama
  print("langchain_ollama is already installed.")
except ImportError:
  print("langchain_ollama is not installed.")
 

try:
  import lxml
  print("lxml is already installed.")
except ImportError:
  print("lxml is not installed.")
 

try:
  import html5lib
  print("html5lib is already installed.")
except ImportError:
  print("html5lib is not installed.")
 

try:
  import python_dotenv
  print("python_dotenv is already installed.")
except ImportError:
  print("python_dotenv is not installed.")